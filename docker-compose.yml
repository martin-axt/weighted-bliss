services:
  weighted-bliss:
    build: .
    container_name: weighted-bliss
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./weighted-bliss.db:/app/weighted-bliss.db
    environment:
      - PORT=3001
      - HOST=0.0.0.0
